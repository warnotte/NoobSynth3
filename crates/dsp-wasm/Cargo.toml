[package]
name = "dsp-wasm"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[package.metadata.wasm-pack.profile.release]
wasm-opt = false

[dependencies]
dsp-graph = { path = "../dsp-graph" }
wasm-bindgen = "0.2"
js-sys = "0.3"
